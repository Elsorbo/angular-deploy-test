
language: node_js
node_js:
  - "14"

script:
  - echo "Omitiendo tests"

cache:
  directories:
    - ./node_modules

after_success:
  - cd client
  - npm install

before_deploy:
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/client
  on:
    branch: master
